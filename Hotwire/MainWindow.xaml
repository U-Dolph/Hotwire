<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotwire"
        xmlns:View="clr-namespace:Hotwire.View" x:Class="Hotwire.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Width="400" Height="600"
        Background="#121212"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10, 100, 10, 100">
            <Label Content="Welcome to HotWire!"
                   HorizontalAlignment="Center"
                   Style="{StaticResource lblStyle}"/>

            <Label Content="Place of logo"
                   HorizontalAlignment="Center"
                   Style="{StaticResource lblStyle}"/>
        </StackPanel>
        
        <StackPanel Grid.Column="1" Grid.Row="1">
            <View:LoginView/>
        </StackPanel>

    </Grid>
</Window>
